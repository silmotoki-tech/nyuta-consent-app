# nyuta-consent-app デザイントークン v1

方向性：静かで事務的、装飾を控えたトーン。手術説明・合併症などの重い内容も
扱うため、ポップな演出や派手な色面は使わない。nyuta-portal/nyuta-karte-logの
ブランド（緑・茶）を承継しつつ、彩度を落として落ち着かせている。

`src/index.css` の `:root` に以下を追加し、既存のTailwindクラス
（`bg-gray-100`等）を、下記トークンを参照する形に順次置き換える。

```css
:root {
  /* ベース */
  --nc-cream: #FBF8F2;       /* 画面全体の背景 */
  --nc-green: #3F6B2C;       /* プライマリ（見出し・主要ボタン）。既存ブランド#6fb82bを落ち着かせた版 */
  --nc-green-soft: #EFF3E8;  /* カード背景・強調カテゴリの下地 */
  --nc-brown: #6b4834;       /* 既存ブランドそのまま。補助見出し・ラベル */
  --nc-ink: #24333F;         /* 本文・署名欄の縁取り */
  --nc-ink-soft: #5B6975;    /* 補助テキスト（カテゴリラベル、日付など） */
  --nc-line: #E4E0D4;        /* 罫線・カード枠 */

  /* 注意喚起（合併症・リスク説明の強調枠のみに使用。多用しない） */
  --nc-mustard: #B98A3D;
  --nc-mustard-bg: #F7F1E4;
}
```

## 使用ルール

- 背景は基本 `--nc-cream` 固定。書類の内容やカテゴリによって背景色を
  変える演出はしない（重い内容の書類だけ画面が暗くなる、等の演出は禁止）
- 強調は色面でなく細い罫線（0.5px程度）で行う。派手な塗りつぶしは
  `--nc-mustard-bg` の1箇所（重要ポイント枠）のみに限定する
- 角丸は8px（カード・ボタン）、署名欄など「特別な場所」は10px
- 影（box-shadow）は使わない。フラットな配色と罫線だけで階層を表現する
- アニメーションは最小限に。保存完了時も派手な演出（バウンド、色フェード、
  スタンプ演出等）は入れず、「保存しました」の静かな表示切り替えのみ

## 画面ごとの適用

**書類選択画面（スタッフ向け）**
- 全体背景 `--nc-cream`
- カテゴリラベルは `--nc-ink-soft`、12px
- カードは `--nc-line` の細枠、角丸8px
- 件数が多いカテゴリ（手術・処置の説明同意書）のみ `--nc-green-soft` 背景で
  視覚的に見つけやすくする。他カテゴリは `--nc-cream` のまま

**説明・サイン画面（飼い主様向け）**
- セクション見出しに番号を振る（①②③…）。ラベルは `--nc-brown`、12px
- タイトルは `--nc-green`、19px、weight 500
- 本文は `--nc-ink`、14px、line-height 1.9（読みやすさ優先）
- 「ここが大事な点です」枠：`--nc-mustard` の細枠＋ `--nc-mustard-bg` 背景。
  1書類につき1箇所程度、多用しない
- 署名欄直前に、確認内容の短い一文（`--nc-ink-soft`、12px）
- 署名欄は `--nc-ink` の枠線、内側に点線のガイド。サインペンの色も
  `--nc-ink` に統一（デフォルトの黒から変更）
- 保存ボタンは `--nc-green` 背景、`--nc-cream` 文字。装飾なし

## 保存完了時の表示（重要：演出を入れない）

- 派手な演出（バウンド、スタンプ、色フェード、感情的なメッセージ）は
  すべて不採用
- 「保存しました」とだけ、簡潔に静かに表示する
- 画面遷移も通常の切り替えのみ。トランジションを目立たせない

## モックアップ

書類選択画面・説明サイン画面の雰囲気は、チャット上で共有したモックアップ
（緑・茶ベース、罫線中心のフラットなデザイン）を参照。
