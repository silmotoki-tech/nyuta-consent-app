# このリポジトリで作業するときの取り決め

## 設計の経緯・不具合ログを読む

作業前に `docs/decisions.md`（設計判断ログ）、`docs/timeline.md`（開発タイムライン）、
`docs/bugs.md`（不具合・課題ログ）に目を通してください。特に以下のケースでは必須です。

- v1（現行実装、React + Firebase + PDF生成）に触れる変更をするとき。PDF生成まわりの
  一見不自然な回避策（`vite.config.js`の`trim-canvas`エイリアスなど）には理由があります
- v2（紙芝居形式への全面再設計）の実装に着手するとき。仕様の大半は実装より先に
  `docs/decisions.md`で固めてあるので、まずそちらを正としてください

設計判断が新たに固まったとき、または不具合を直したときは、実装の変更と同じタイミングで
該当ファイルに追記してください（`docs/decisions.md` は判断と理由、`docs/bugs.md` は
症状と対応、`docs/timeline.md` は日付と出来事を1行）。会話の中で決まったことをその場で
書き残さないと、次の作業のときに経緯が失われるため。

## 新しい承諾書を追加するとき

v2の承諾書は `src/documents/<書類ID>.js` に1書類=1ファイルで定義し、
紙芝居スライドはアプリ内のテキストとして組みます（画像は使いません）。
一般手術注意事項（`src/documents/ippan-shujutsu.js`）が基準の実装なので、
新規追加時はまずそれを読んでください。

### 手順

1. 紙の原本から全文を書き起こす（要約・言い換えはしない）
2. 書き起こした内容を「情報の性質」で分類し、スライドの構成を決める
3. `src/documents/<書類ID>.js` を作成する
4. `src/documents/index.js` の書類一覧に登録する

### スライドの内容を決める原則

**情報の性質で分類する。** 時系列や場面で分けない。その情報が何のために
あるかで括ると、説明する側も「今何を伝えているか」が明確になります。

一般手術注意事項での分類例：

| 分類 | 内容 |
|---|---|
| 守らないと処置ができない | 絶食のお願い |
| 守らないと費用が発生する | 予防未実施・当日キャンセルのペナルティ |
| 忘れ物・準備の抜けを防ぐ | フード持参、排便排尿 |
| 事前告知でクレームを防ぐ | 待ち時間、毛刈り |

手術説明の書類なら「目的」「術式」「術後の経過」「合併症のリスク」と
いった分類になります。原本の見出しをそのまま使えることも多いですが、
見出しがない場合はこの観点で括り直してください。

**各スライドは主文＋補足の構造にする。** 理由や例外を述べた文を主文と
同列の箇条書きにすると、独立した指示のように見えてしまいます。

悪い例（3つの独立した指示に見える）：
- 前日の22時以降は食事をとらないでください
- 食べたものを吐いてしまうと危険なため、必ずお守りください
- 水はご来院前まで飲んでいただいて大丈夫です

良い例（主従が明確）：
- 主文：前日の22時以降は食事をとらないでください
- 補足：食べたものを吐いてしまうと危険なため、必ずお守りください／
  水はご来院前まで飲んでいただいて大丈夫です

**枚数に上限はありません。** 1枚に詰め込むより、分けた方が読みやすい
場合は分けてください。ただし1枚1メッセージを基本とします。

### slides の書き方

ブロックは3種類だけです。

| type | 用途 |
|---|---|
| `main` | 主文（大きく表示）＋ `notes`（補足、左罫線でぶら下げ） |
| `list` | 並列の項目を丸ドット付きで並べる |
| `divider` | 区切り線。1枚に主文が複数あるときだけ使う |

```js
slides: [
  {
    id: '<書類ID>_01',
    category: 'スライドのカテゴリ名',
    emphasis: true, // 最重要スライドのみ true。バッジがマスタード系になる
    blocks: [
      {
        type: 'main',
        text: '主文。<em>強調したい語</em>はemで囲む（マスタード色になる）',
        notes: ['補足1。', '補足2。'],
      },
    ],
  },
]
```

金額・時刻・確率など、**読み落とすと困る数値は `<em>` で囲む**のが
基準です。装飾を増やさず、文字色だけで目を引かせます。

### fullText の書き方

渡す書類（印刷される1枚）に使う全文です。**原本の文言をそのまま使い、
要約や言い換えはしません**（証拠になる書類のため）。

ただし、実際の処置内容と原本の記載が食い違っている場合は、実態に
合わせて追記してください（例：一般手術注意事項では、麻酔のカテーテル
設置のため腕にも毛刈りが必要な旨を追記した）。その場合は
`docs/decisions.md` に理由を残してください。

### 書類を追加したら

`docs/timeline.md` に日付と追加した書類名を1行書いてください。
分類の判断に迷った点や、原本から変更した箇所があれば
`docs/decisions.md` にも残してください。

## その他

- 応答は日本語で行う。
