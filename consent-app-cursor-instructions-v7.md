# nyuta-consent-app 次の実装指示 v7

これまでの静かなトーンは変えず、視覚的な手がかり（アイコン・余白・番号
バッジ）を足して見やすくする。あわせて、書類名の変更と表示順の調整を行う。

---

## 1. 視覚的な肉付け（デザイン微調整）

### 1-1. 書類選択画面
- 各カテゴリの見出し（例：「入院・お預かり」）の左に、小さなアイコン
  （16px程度、`lucide-react`から。カテゴリの雰囲気に合うものを選んでよい。
  例：手術系は`Activity`、入院系は`Home`、予約系は`Calendar`）を追加
- 各カードの中に、カテゴリと同じアイコンを18px程度で上部に配置し、
  タイトルとの間に余白を持たせる（縦積みレイアウト：アイコン→タイトル）
- カードの角丸を8px→9px、内側の余白（padding）を少し広げる
  （`p-3`程度→`p-4`程度）

### 1-2. 説明・サイン画面（本文表示）
- セクション見出しの「①②③」の小さいテキストラベルを廃止し、代わりに
  丸い番号バッジ（26px程度の円、背景`--nc-green-soft`、文字色
  `--nc-green`、中央揃えの数字）に変更
- バッジと本文の間は横に余白を取ったレイアウト（バッジが左、右側に
  タイトル＋本文）にする
- セクション間の余白（margin-bottom）を広げ、ブロックごとの区切りを
  はっきりさせる
- `checklist`の箇条書きマーカーを、ブラウザ標準の「・」から、5px程度の
  小さな丸ドット（`--nc-brown`色）に変更し、行間も少し広げる
- 「ここが大事な点です」枠に、小さな注意アイコン（`AlertCircle`など、
  16px、`--nc-mustard`色）をタイトルの左に追加

これ以外の色・装飾（影、派手な演出等）は追加しない。あくまで余白と
アイコンによる整理にとどめる。

---

## 2. 書類名の変更

`formTypes.js` の `label` を以下の通り変更する（`id`はそのまま）。

| 対象書類（現在のlabel） | 変更後のlabel |
|---|---|
| 手術予定 | 一般手術注意事項 |
| 検査予定 | 検査注意事項 |
| 承諾書（一般） | 入院・預かり承諾書 |
| お預かりにあたってのご注意 | ペットホテル承諾書 |
| 前十字靭帯断裂整復手術（十字靭帯） | 十字靭帯断裂手術 |
| 軟口蓋切除・外鼻孔拡張手術 | 軟口蓋切除術 |
| 膝蓋骨脱臼整復手術 | 膝蓋骨脱臼整復術 |

書類選択画面のカードに表示される文字列も、上記の新しい`label`に
自動的に反映されるはず（`label`を参照している箇所のみの変更で足りる）。

---

## 3. カテゴリの表示順変更

書類選択画面でのカテゴリの並び順を、以下の順に変更する。

1. 入院・お預かり
2. 予約案内
3. 手術・処置の説明同意書（5件：胆嚢摘出術・軟口蓋切除術・十字靭帯断裂
   手術・膝蓋骨脱臼整復術・抗がん剤副作用）

「手術・処置の説明同意書」カテゴリを一番下に配置する（これまでは一番上
だった）。カテゴリ内の書類の並び順自体は変更しなくてよい。

---

## 完了の定義
- [ ] 選択画面にカテゴリアイコン・カードアイコンが表示され、余白が広がっている
- [ ] 本文画面のセクション見出しが丸い番号バッジになっている
- [ ] チェックリストのマーカーが丸ドットになっている
- [ ] 重要ポイント枠にアイコンが付いている
- [ ] 上記7件の書類名が変更されている
- [ ] カテゴリの表示順が「入院・お預かり→予約案内→手術・処置の説明同意書」になっている
- [ ] ビルド・デプロイして公開URLで確認済み
