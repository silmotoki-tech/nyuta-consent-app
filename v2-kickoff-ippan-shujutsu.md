# nyuta-consent-app v2 実装キックオフ

`docs/decisions.md`・`docs/timeline.md`・`docs/bugs.md`に仕様検討の経緯が
まとまっている。本ドキュメントは、それを踏まえて**最初の1書類（一般手術
注意事項）を実際に動く形にする**ための実装指示。

作業前に必ず`AGENTS.md`の指示通り`docs/decisions.md`・`docs/bugs.md`に
目を通すこと。v1のPDF生成方式（`html2pdf.js`関連）には触れない。

---

## 0. 前提（`docs/decisions.md`からの要約）

- PDF生成ライブラリは使わない。印刷はHTML＋`window.print()`
- 保存はPDFではなくデータ（署名画像・入力内容・チェック記録・
  スライドのバージョン参照・印刷日時）
- スライドはGoogleスライドから書き出したPNG画像。アプリに登録する機能は
  作らない（開発者が直接ファイルを配置する）
- 全書類「紙芝居形式」に統一。1枚ずつめくって理解チェック、最後に署名

---

## 1. 対象書類とスライド素材

**一般手術注意事項**を最初の実装対象とする。

スライド素材（今回添付、内容は仮。文言の細部は後日調整予定）：

1. 当日ご持参いただくもの・お願い
2. 前日夜からの注意事項
3. 予防とその他注意事項について

3枚とも16:9のPNG画像。`src/assets/slides/ippan-shujutsu/01.png` のような
形でリポジトリに配置する（配置場所はプロジェクトの慣例に合わせてよい）。

**注意**：スライド2「前日夜からの注意事項」に「食べたものを吐いてしまうと
危険なため」という理由の一文が抜けている。今回は仮素材として実装を進めて
よいが、後日画像を差し替える前提でスライドIDとファイル名を分離しておく
こと（内容更新時に参照側のコードを変えずに済むように）。

---

## 2. データ構造

### 2-1. 書類定義

`v1`の`formTypes.js`のような一枚岩の配列ではなく、書類ごとに
「スタート画面での選択肢」「スライド一式」「渡す書類のテキスト」を
まとめて持つ構造にする。

```js
// src/documents/ippan-shujutsu.js のようなイメージ
export const ippanShujutsu = {
  id: 'ippan_shujutsu',
  label: '一般手術注意事項',
  category: 'reservation', // v1のカテゴリ分類を踏襲
  requiresResuscitationChoice: false, // 蘇生処置の選択が必要な書類はtrue

  // スタート画面で選ぶ、この書類に関係する項目
  procedureOptions: [
    '去勢手術', '避妊手術', '歯石除去', '乳腺腫瘍切除', '抗がん剤治療', 'その他',
  ],

  // 紙芝居のスライド（内容固定、画像）
  slides: [
    { id: 'ippan_shujutsu_01', image: '/assets/slides/ippan-shujutsu/01.png' },
    { id: 'ippan_shujutsu_02', image: '/assets/slides/ippan-shujutsu/02.png' },
    { id: 'ippan_shujutsu_03', image: '/assets/slides/ippan-shujutsu/03.png' },
  ],
  slideVersion: 'v2026-09-08', // 改訂時にここだけ変更する

  // 渡す書類・控えに使う全文テキスト（原文ベース、アプリ側でHTML組版）
  fullText: {
    noticeBar: '混雑や診察状況により当日お預かりに時間がかかることがあります。お時間に余裕をもってお越しください。',
    items: [
      '前日の22時以降は食事(水以外)をとらないでご来院ください。（食べたものを吐いてしまうと危険なため）水はご来院前まで飲んでも大丈夫です。',
      '普段食べているフードを＿＿＿回分ご持参ください。',
      '可能であれば、排便排尿をしてからご来院ください。',
      '１か月以内のノミ・ダニの予防と１年以内のワクチン接種が必要です。(予防がない場合は隔離お預かりの料金が発生します)',
    ],
    footnotes: [
      '当院で処方の予防薬を使用していない場合に、ノミやダニを発見した場合は駆虫と施設消毒費用(6000円)＋予防薬の費用を頂きます。',
      '手術にあたっては手術部位の毛刈りが必要になります。',
      '当日キャンセルの場合は予約変更手数料として3000円かかります。絶食指示を必ず守ってご来院ください。',
    ],
  },
};
```

`requiresResuscitationChoice: true`にする書類（麻酔および手術における
合併症について、を実装するときに使う）は、まだ未実装でよい。今回は
falseの書類から始める。

### 2-2. セッション中の入力データ

スタート画面〜出力までの一連の状態を1つのオブジェクトで持つ。

```js
{
  ownerName: '',
  petName: '',
  karteNumber: '', // v1では廃止判断があったため要確認。docs/decisions.md参照
  date: '',
  visitTime: '',
  selectedDocumentIds: [], // スタート画面で複数選択
  procedureSelections: {}, // 書類IDごとの選択内容
  // 書類ごとの進行データ（複数書類を連続処理するため配列で保持）
  documentSessions: [
    {
      documentId: 'ippan_shujutsu',
      slideCheckTimestamps: {}, // { slideId: timestamp }
      resuscitationChoice: null,
      signatureDataUrl: null,
      signedAt: null,
      explainerStaff: null, // 最終確認画面で設定
      scheduleEntryStaff: null,
      scheduleEntryChecked: false,
      printedAt: [], // 印刷操作の履歴（複数回対応）
    },
  ],
}
```

---

## 3. 画面フロー（この書類での具体例）

1. **スタート画面**：飼い主名・動物の名前・日付・来院時間・書類選択
   （一般手術注意事項にチェック）・手術内容の選択（避妊手術等）
2. **確認画面**：「本日のご説明」として、選んだ内容を表示。チェック不要、
   「次へ」で進む
3. **紙芝居**：スライド1〜3を順に表示。各スライド下部に「理解しました」
   チェックボックス。未チェックでも「次へ」は押せる
4. **蘇生処置の選択**：この書類は`requiresResuscitationChoice: false`
   なのでスキップ
5. **署名画面**：3枚すべてチェック済みでなければ「署名する」ボタンを
   無効化する。チェック未了の場合、どのスライドが未チェックか分かる
   表示にする（例：「2枚目が未確認です」）
6. **最終確認画面**：説明した獣医師／予定表に記入した人を選択、
   「予定表に記入した」チェック。すべて揃うまで「保存」を無効化
7. **出力**：
   - 確認記録付き署名画像を生成（署名canvas＋各スライドのチェック時刻＋
     蘇生処置選択（該当なしなので省略）＋説明者・記入者名を1枚に合成）
   - 渡す書類用のHTML（A4相当のレイアウト）を組み立て、`fullText`の
     内容＋入力値＋確認記録付き署名画像を表示
   - `window.print()`で印刷
   - Firestoreに保存：入力値・チェック時刻・署名画像（Storageに保存し
     てURLを保存）・スライドバージョン参照・説明者/記入者・印刷日時

---

## 4. 今回のスコープ

- この1書類（一般手術注意事項）が最初から最後まで動くこと
- 複数書類の連続処理は、まだ実装しなくてよい（1書類の単体フローが
  安定してから着手）
- スタッフ名の一覧は、いったんコード内に配列で直書きしてよい
  （院内メンバーが決まっているため。将来的な変更は別issueとする）

## 5. 完了の定義

- [ ] スタート画面で入力→確認画面→紙芝居3枚（チェック機能つき）→
      署名→最終確認→出力、が一気通貫で動く
- [ ] 未チェックのまま署名画面に進んだ場合、署名できない
- [ ] 最終確認が揃わないと保存・出力できない
- [ ] 渡す書類のHTMLに入力値・確認記録付き署名画像が正しく反映される
- [ ] Firestoreに保存され、内容が確認できる
- [ ] `docs/decisions.md`・`docs/timeline.md`に、実装中に生じた新しい
      判断・出来事を追記する
